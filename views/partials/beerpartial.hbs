<img src="{{this.image_url}}" alt="beer">
<div class="single-container">
    <h2><a href="/beers/{{this.id}}">{{this.name}}</a></h2>
    <p class="partial-tagline">{{this.tagline}}</p>
    <p class="partial-description">{{this.description}}</p>
    
</div>